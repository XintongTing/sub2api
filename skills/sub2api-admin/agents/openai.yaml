interface:
  display_name: "OneAPI Admin"
  short_description: "Manage OneAPI admin accounts and settings"
  default_prompt: "Use $sub2api-admin to inspect and manage OneAPI backend accounts safely."

policy:
  allow_implicit_invocation: true
